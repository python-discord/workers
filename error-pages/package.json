{
  "name": "error-pages",
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "format": "prettier --write .",
    "lint": "prettier --check ."
  },
  "devDependencies": {}
}
